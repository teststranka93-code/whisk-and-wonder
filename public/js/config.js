// Central config for API base + GA4 measurement id placeholder.
window.WW_CONFIG = {
  API_BASE: window.location.origin,
  GA_MEASUREMENT_ID: "G-XXXXXXXXXX", // replace with real GA4 ID before launch
};
